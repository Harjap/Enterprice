@model IEnumerable<assignment.Models.Game>

@{
    ViewBag.Title = "My Games";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>My Games</h2>


<ul>
    @foreach (var item in Model)
    {
    <li>

        @Html.ActionLink(item.Name, "Details", new { id = item.GamesID })
    </li>
    }
</ul>

